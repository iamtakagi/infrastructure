. {
    whoami
    forward . 1.1.1.1 8.8.8.8 100.100.100.100
    errors
    log . "{proto} {remote} is Request: {name} {type} {>id}"
    hosts /etc/coredns/hosts {
        fallthrough
    }
    reload
}